def main():
    # Solicitar ao usuário que insira um número
    numero = int(input("Digite um número inteiro: "))

    # Iterar de 1 a 10 usando um laço for
    for i in range(1, 11):
        produto = numero * i
        print(f"{numero} x {i} = {produto}")

if __name__ == "__main__":
    main()
